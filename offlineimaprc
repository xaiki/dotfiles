# -*- conf -*-
[general]
ui = Noninteractive.Basic
accounts = GMail, Croll
#, Smartjog
maxsyncaccounts = 10
pythonfile = ~/.offlineimap-scripts/offlineimap.py

[Account GMail]
localrepository = LocalIMAP
remoterepository = GMailRemote
autorefresh = 1

[Repository LocalIMAP]
type = IMAP
remotehost = localhost
port = 143
preauthtunnel = /usr/sbin/dovecot --exec-mail imap

[Repository GMailRemote]
type = Gmail
#remoteuser = 0xa1f00@gmail.com
remoteusereval = get_username("imap.gmail.com")
remotepasseval = get_password("imap.gmail.com")
# remotepass = super_secret_password
holdconnectionopen = true
keepalive = 60
nametrans = lambda foldername: re.sub('^', 'Gmail.', foldername)
folderfilter = lambda foldername: re.search('INBOX', foldername)

#Setting realdelete = yes will Really Delete email from the server.
#Otherwise "deleting" a message will just remove any labels and
#retain the message in the All Mail folder.
realdelete = no

[Account Sagem]
localrepository = LocalIMAP
remoterepository = SagemRemote
autorefresh = 1

[Repository SagemRemote]
type = IMAP
remotehost = 10.67.1.6 #ruelm01sc.ads.local
remoteusereval = get_username("ruelm01sc.ads.local")
remotepasseval = get_password("ruelm01sc.ads.local")
ssl = no
holdconnectionopen = true
keepalive = 60
nametrans = lambda foldername: re.sub('^', 'Sagem.', foldername)

#Setting realdelete = yes will Really Delete email from the server.
#Otherwise "deleting" a message will just remove any labels and
#retain the message in the All Mail folder.
realdelete = no

[Account Croll]
localrepository = LocalIMAP
remoterepository = CrollRemote
autorefresh = 1

[Repository CrollRemote]
type = IMAP
remotehost = imap.croll.fr
remoteusereval = get_username("imap.croll.fr")
remotepasseval = get_password("imap.croll.fr")
ssl = yes
holdconnectionopen = true
keepalive = 60
nametrans = lambda foldername: re.sub('^', 'Croll.', foldername)

#Setting realdelete = yes will Really Delete email from the server.
#Otherwise "deleting" a message will just remove any labels and
#retain the message in the All Mail folder.
realdelete = no

[Account Smartjog]
localrepository = LocalIMAP
remoterepository = SmartjogRemote
autorefresh = 1

[Repository SmartjogRemote]
type = IMAP
remotehost = imap-eu.smartjog.net
remoteuser = nsardi
ssl = yes
# remotepass = super_secret_password
holdconnectionopen = true
keepalive = 60
nametrans = lambda foldername: re.sub('^', 'SmartJog.', foldername)
#Setting realdelete = yes will Really Delete email from the server.
#Otherwise "deleting" a message will just remove any labels and
#retain the message in the All Mail folder.
realdelete = no
